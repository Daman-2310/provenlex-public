# Genesis Swarm v0.4.0 — Cloud Production Dependencies
# Used by Railway (backend) and pip installs
# Excludes torch/shap/sentence-transformers (too heavy for cloud free tier)

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
websockets>=12.0

# ── AI ────────────────────────────────────────────────────────────────────────
anthropic>=0.40

# ── Auth / Security ────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
cryptography>=42.0

# ── HTTP clients ───────────────────────────────────────────────────────────────
httpx>=0.27
aiohttp>=3.9
certifi>=2024.0
defusedxml>=0.7

# ── Data / ML (lightweight) ───────────────────────────────────────────────────
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
yfinance>=0.2
joblib>=1.3.0

# ── CLI / Display ─────────────────────────────────────────────────────────────
click>=8.1
rich>=13.0
pydantic-settings>=2.0

# ── PDF report generation ─────────────────────────────────────────────────────
reportlab>=4.0
langdetect>=1.0.9
pypdf>=4.0
rank-bm25>=0.2.2

# ── Structured logging ────────────────────────────────────────────────────────
structlog>=24.0

# ── Database migrations ───────────────────────────────────────────────────────
alembic>=1.13
sqlalchemy>=2.0

# ── Vector memory (disabled on cloud — falls back to in-memory dict) ──────────
# chromadb>=0.5  # too heavy for free-tier (downloads 79MB ONNX model → OOM)

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Observability (disabled on cloud — port conflicts with uvicorn) ────────────
# prometheus-client>=0.20

# ── Blockchain anchoring (optional — no-op if not installed) ─────────────────
# web3>=6.0           # Ethereum calldata anchoring
# grpcio>=1.63        # distributed PBFT (see consensus/grpc/)
# grpcio-tools>=1.63  # generate proto stubs: python -m grpc_tools.protoc ...

# ── XAI (optional — falls back to feature_importance_ proxy) ─────────────────
# shap>=0.45          # enable via pyproject.toml [shap] extra

# ── OpenTelemetry (optional — no-op if not installed) ─────────────────────────
# opentelemetry-sdk>=1.24
# opentelemetry-exporter-otlp>=1.24

# ── Dev / test extras (installed via pyproject.toml [dev]) ────────────────────
# pytest pytest-asyncio hypothesis bandit safety semgrep pip-audit
