[flake8]
max-line-length = 100
extend-ignore = E203, W503, E402
per-file-ignores =
    src/genesis_swarm/api/server.py: E501
    src/genesis_swarm/api/pitch_deck.py: E501

[isort]
profile = black
line_length = 100

[tool:pytest]
testpaths = tests
asyncio_mode = auto
addopts = --tb=short -q
